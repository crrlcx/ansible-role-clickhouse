{{ ansible_managed | comment('xml') }}
<?xml version="1.0"?>
<yandex>
  <macros>
{% for key, value in clickhouse_macros.items() | sort %}
    <{{ key }}>{{ value }}</{{ key }}>
{% endfor %}
  </macros>
</yandex>
