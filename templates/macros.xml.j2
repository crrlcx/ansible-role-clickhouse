<?xml version="1.0"?>
<!-- {{ ansible_managed }} -->
<yandex>
    <macros>
{%  for key, value in clickhouse_macros.items()|sort %}
        <{{ key }}>{{ value }}</{{ key }}>
{%  endfor %}
    </macros>
</yandex>
